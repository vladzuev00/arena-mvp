spring:
  datasource:
    url: jdbc:tc:postgresql:11.1:///integration-tests-db
  jpa:
    hibernate:
      ddl-auto: none
      properties:
        hibernate:
          generate_statistics: true
          jdbc:
            batch_size: 2
          dialect: org.hibernate.dialect.PostgreSQL94Dialect
    show-sql: true
  sql:
    init:
      mode: always
  kafka:
    bootstrap-servers: 127.0.0.1:9092

kakava-client:
  client-id: 123e4567-e89b-42d3-a456-556642440000
  grant-type: password
  username: testuser@kakava.lt
  password: testpassword
  base-url: https://app-kkv-be-test.azurewebsites.net
  auth-path: /Token
  shows-path: /event/show
  tickets-path: /order
  using-ticket-path: /arena/admission